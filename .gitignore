result
result-*
/docs/_build
__pycache__/
*.pyc
.pytest_cache/
.pytest-agent/
.venv/
*.egg-info/
dist/
build/

# `ekn tofu` working directories: .terraform/ and, for a local backend, live
# state. Per unit, created beside whatever configuration was evaluated.
.ekn/

# Nix / direnv
.direnv/

# Generated by `nix flake lock`, and not committed: it would pin flake
# consumers to one umbrella revision, which is a decision of its own.
# nixkube and nanopynix ignore their own for the same reason.
/flake.lock

# Personal Claude Code settings
.claude/settings.local.json
